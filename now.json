{
    "name": "pos-api",
    "alias": ["api.dexpress.app"],
    "version": 2,
    "builds": [{ "src": "src/index.ts", "use": "@now/node" }],
    "routes": [{ "src": "^/(.*)", "dest": "src/index.ts" }],
    "github": {
        "enabled": true,
        "autoAlias": false
    },
    "env": {
        "HOSTDB": "@host-db"
    }
}